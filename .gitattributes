# Normalise to LF in the repo so Windows and Unix contributors stop
# generating whitespace-only diffs against each other.
* text=auto eol=lf

*.ipynb text eol=lf
Makefile text eol=lf
*.png binary
*.jpg binary
*.pdf binary
